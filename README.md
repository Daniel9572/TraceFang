# Market Analysis Platform

面向多市场、多品种和多数据源的交易分析平台。第一阶段用现货黄金与现货白银验证结构化采集、可信存储、按合约来源管理和 K 线终端。

## 第一版已经具备

- 自有品牌的桌面行情工作台：品种观察、实时报价、当前 K 线实时生长、收盘倒计时、当前价聚焦、缩放/平移与 1/5/15/30/60 分钟聚合；
- 每个合约独立保存一个实时行情产品；产品可以是单通道，也可以声明固定字段归属的显式组合源；
- 金十客户端组合行情固定由官网通道提供实时价格、桌面会话通道提供日内补充字段，两个原始通道分别落库且绝不静默互相代替；
- 全局历史 K 线统一从 PostgreSQL 的验收表读取；原始候选先经过结构、时序和多源价格一致性校验，缺口再由后台优先使用免费通道补齐；
- 实时请求或订阅只绑定一个明确来源，断线时标记停滞，不自动换源；
- 浏览器通过 WebSocket 接收持续报价事件；打开页面、REST 查询和新增订阅者都只读本地最新帧，不触发上游调用；
- PostgreSQL 保存合约来源路由、分渠道原始载荷、报价事件、最新报价、候选 K 线、校验结果和标准 K 线；
- 来源管理：启停、优先级、能力、健康状态、运行要求和连通测试；
- 金十官方 MCP 适配代码与配置项仅作归档保留；当前冻结，不初始化，也不作为实时、历史或校对数据源；
- 金十官网高速行情：直接接收金十官网公开结构化 WebSocket 的价格变化推送，无需 Token 或桌面客户端；
- 金十桌面会话行情：使用本机金十客户端会话鉴权并解码 WebSocket 二进制报价，支持心跳、断线重连和源时间；截图与 OCR 已从生产代码中删除；
- 通用领域模型、能力端口和适配器边界，方便继续接入期货、股票、外汇、加密资产和其他供应商。

## 数据来源

| 来源 | 首版能力 | 优点 | 运行要求 |
| --- | --- | --- | --- |
| 金十客户端组合行情 | 现价/涨跌 + 日内统计 | `jin10_web` 与 `jin10_local` 固定字段归属，兼顾实时性与字段完整度 | 首次需登录金十软件生成桌面会话；之后由本应用按合约连接 |
| 金十官方 MCP（冻结） | 能力代码归档保留 | 当前不产生任何网络调用 | `JIN10_MCP_ENABLED=false`；不可选、不可测试、不参与历史 |
| 金十官网高速原始通道 | 现货黄金、现货白银实时价格 | 结构化、变化驱动、不消耗 MCP 额度 | 无需 Token 或桌面客户端；公开通道无正式 SLA |
| 金十桌面会话原始通道 | 日内统计、买卖价及原始报价 | 结构化推送，不消耗 MCP 工具额度 | 首次需登录金十软件生成本机会话；连接建立后软件无需保持运行 |

XAUUSD 可以使用金十客户端组合行情，同时 XAGUSD 独立使用金十官网高速原始通道。每个合约的采集路由独立计算；组合源的价格通道失效时会停滞该合约，不会改用补充通道的价格。两者的历史 K 线仍读取同一套全局标准历史。本地结构化研究路线见 [docs/local-data-acceleration.md](docs/local-data-acceleration.md)。

## 快速开始

首次运行，双击 `setup.cmd` 安装并构建，之后双击 `start.cmd` 启动。

`setup.cmd` 会在被 Git 忽略的 `.env.local` 中生成本机 PostgreSQL 随机凭据；`start.cmd` 会在 Docker Desktop 已运行时启动项目专用 PostgreSQL 容器。

复制本机环境文件模板后只填写当前启用的本地会话配置。MCP 保持 `JIN10_MCP_ENABLED=false`，不要为运行本项目启用或测试它：

```powershell
Copy-Item .env.example .env
# 用文本编辑器填写本地会话配置；不要把真实 Token 写回 .env.example
.\start.cmd
```

本地结构化报价使用 `JIN10_LOCAL_SESSION_TOKEN`。令牌写入 `.env` 后，用户 ID 默认从最新金十登录日志自动识别；也可通过 `JIN10_LOCAL_USER_ID` 显式配置。任何 Token 都不得写入代码、测试或可提交文件。

配置优先级为：进程环境变量 > `.env.local` > `.env`。`.env.local` 保存本机生成的数据库凭据，`.env` 保存人工维护的 Token，二者都被 Git 忽略。

浏览器会打开 `http://127.0.0.1:8000`。也可以完全从终端安装和启动：

```powershell
uv sync --python 3.13
corepack pnpm -C web install --frozen-lockfile
corepack pnpm -C web build
uv run market-analysis-server
```

真实 Token 不得写入 `.env` 以外的非忽略文件、代码、测试、日志或 Git 历史。若 Token 曾粘贴到聊天或其他可留存位置，建议在服务端重新生成。

## 数据源使用方式

- 实时路由粒度是“合约”。切换合约时恢复该合约自己的实时行情源。
- 所有实时来源选择都是强制来源，失败时直接报告，不暗中换源。
- `测试连接` 只适用于当前启用的免费结构化通道。MCP 卡片明确显示“已冻结”，其启停、选择和测试按钮均锁定。
- 实时采集由后端合约路由常驻管理，与浏览器订阅数量无关。推送通道只订阅实际分配给它的合约；启动时会把遗留的 MCP 合约路由迁移到默认组合产品。
- 实时 REST/WebSocket 首帧只读内存或 PostgreSQL `latest_quotes`，不会调用任何上游。用户主动“测试连接”是明确的例外，并会把所得原始帧按真实渠道缓存、落库。
- 历史 K 线只从本地 PostgreSQL 的 `standard_candles` 读取，与合约实时源无关。后台缺口补齐仅使用免费通道；任何包含 MCP 证据的标准历史和校验结果都会被清除并在查询层拒绝。
- 当前柱在浏览器中用“全局历史基线 + 当前合约实时源报价”合成：同秒到达的多帧全部保留并参与开高低收，跨周期后由下一笔报价开启新柱。倒计时每秒更新，不额外调用数据接口。
- 浏览器通过 WebSocket 持续接收本地事件；MCP 服务当前不会创建客户端、握手、轮询或执行任何工具调用。
- 本地金十源由上游事件驱动，当前实测通常约 3～4 秒收到一帧，不做 0/5/10 秒固定轮询，也不会在断线时混入官方源或截图数据。
- 金十官网高速行情归类为“优质分析级”：使用公开网页行情协议的变化驱动档；2026-08-06 实测黄金 30 秒收到 70 帧，平均约 3.68 帧/秒；本机后端接收至网页采样器平均 1.27ms。它适合常规分析，但不等同于有明确低延迟 SLA 的机构专业级数据，也不与本地会话源静默混合。
- 图表工具栏的来源下拉框同时承担管理控制台：可直接启停、测试、查看额度与运行条件，并为当前合约切换实时源，不再打开独立管理页面。

合约的实时行情源保存在 PostgreSQL 的 `instrument_source_routes` 兼容表 `quote` 行中；旧 `candles` 行不再参与历史读取。来源启停和页面顺序保存在 `data/sources.json`。接口契约见 [docs/source-management.md](docs/source-management.md)。

## PostgreSQL

本地容器只绑定 `127.0.0.1:54329`。原始报价先进入内存事件通道并推送页面，再由有界队列异步写入 PostgreSQL，不把数据库延迟放进实时报价热路径。`quote_events`、`latest_quotes` 与 `candles` 是分渠道证据层；`candle_validation_results` 记录每分钟接受/拒绝及证据；只有 `standard_candles` 供历史查询。路由保存在 `instrument_source_routes`。

## 验证

```powershell
uv run python -m unittest discover -s tests -v
uv run ruff check .
uv run ruff format --check .
corepack pnpm -C web typecheck
corepack pnpm -C web build
```

实时 MCP 契约测试作为归档能力保留，并被 `JIN10_MCP_ENABLED=false` 双重锁定；当前测试流程不得启用它。

## 重要资产边界

`XAUUSD` 和 `XAGUSD` 是现货黄金、现货白银，不是黄金/白银期货。它们可以作为黄金期货分析的宏观和跨市场输入，但不能替代 COMEX、上期所等具体期货合约的价格、基差、期限结构、持仓量与换月数据。

总体设计见 [docs/architecture.md](docs/architecture.md)，冻结的 MCP 历史合同见 [docs/jin10-mcp.md](docs/jin10-mcp.md)。
