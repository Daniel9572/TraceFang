# NATS has no max_payload CLI flag. Keep this native server limit aligned with
# MARKET_ANALYSIS_NATS_MAX_FRAME_BYTES used by the application and Compose.
max_payload: $MARKET_ANALYSIS_NATS_MAX_FRAME_BYTES
